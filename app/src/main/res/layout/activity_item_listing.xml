<!--This is the layout for the Feed Activity. This is a relative layout which means that
items are placed relative to the other items around it-->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="12dp"
    android:paddingRight="15dp"
    android:paddingTop="12dp"
    android:paddingBottom="15dp"
    android:baselineAligned="true"
    tools:context=".FeedActivity">

    <!-- Sets the background of the layout. Sets the background color
  to light purple-->
    <View
        android:layout_width="fill_parent"
        android:layout_height="2dp"
        android:background="#c0c0c0"
        android:id="@+id/divider" />

<!-- listview for a scrollable layout-->
    <ListView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/list"
        android:layout_marginTop="8dp"
        android:layout_below="@+id/divider" />

<!-- button for selling-->
    <Button
        style="?android:attr/buttonStyleSmall"
        android:textColor="#ffffff"
        android:background="#FF5F446E"
        android:layout_width="80dp"
        android:layout_height="35dp"
        android:text="Sell"
        android:id="@+id/sellButton"
        android:layout_alignParentBottom="true"
        android:layout_alignRight="@+id/list"
        android:layout_alignEnd="@+id/list" />

</RelativeLayout>
